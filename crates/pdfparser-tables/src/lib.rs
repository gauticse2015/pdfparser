//! Table extraction (Phase T stub — Phase U/V implement strategies).
#![deny(missing_docs)]

/// Placeholder so workspace compiles; tables default off.
pub fn tables_available() -> bool {
    false
}
